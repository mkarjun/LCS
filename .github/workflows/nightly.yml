name: Nightly

# Builds and publishes native images from the tip of main as nightly Docker tags.
# Runs every night at 22:00 CT (03:00 UTC, approximate — shifts 1 h in CST winter).
# Can also be triggered manually.
#
# Published tags (LCS_REPO below):
#   Native:  <repo>:nightly          <repo>:nightly-mmddyyyy
#   Compat:  <repo>:nightly-compat   <repo>:nightly-mmddyyyy-compat
#
# Required secrets: DOCKERHUB_USERNAME, DOCKERHUB_TOKEN
#
# Upstream also mirrored every nightly to public.ecr.aws under its own account.
# That needed an AWS role LCS does not have, so the mirror is gone rather than
# left to fail every night; Docker Hub is the single home. Restoring it means
# adding back an OIDC role, an ECR login, and `id-token: write` here.

on:
  schedule:
    - cron: '0 23 * * *'
      timezone: 'America/Chicago'
  workflow_dispatch:        # Allow maintainers to trigger manually

permissions:
  contents: read

env:
  # Kept in step with release.yml -- both publish to the same repository.
  LCS_REPO: ${{ vars.DOCKERHUB_REPO || 'mkarjun/lcs' }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Compute nightly version (CT date) ────────────────────────────────────
  prepare:
    name: Prepare nightly version
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule' || !github.event.repository.fork
    outputs:
      version: ${{ steps.date.outputs.version }}

    steps:
      - name: Set date version (CT)
        id: date
        run: echo "version=$(TZ='America/Chicago' date +'%m%d%Y')" >> "$GITHUB_OUTPUT"

  # ── Build native artifact — amd64 ────────────────────────────────────────
  build-native-amd64:
    name: Build native artifact (amd64)
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v7.0.1
        with:
          ref: main

      - uses: graalvm/setup-graalvm@5298d94fb55a4f185c602eeac5de1b553882abe2 # v1
        with:
          java-version: '25'
          distribution: 'mandrel'
          cache: maven
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set nightly version
        run: mvn versions:set -DnewVersion="nightly-${{ needs.prepare.outputs.version }}" -DgenerateBackupPoms=false -q

      - name: Build native executable
        run: mvn clean package -Dnative -DskipTests -B -Dquarkus.native.additional-build-args-append="-march=x86-64-v2"

      - uses: actions/upload-artifact@v7
        with:
          name: native-amd64
          path: |
            target/*-runner
            target/*.properties
            target/*.so
          if-no-files-found: warn
          retention-days: 1

  # ── Build native artifact — arm64 ────────────────────────────────────────
  build-native-arm64:
    name: Build native artifact (arm64)
    needs: prepare
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v7.0.1
        with:
          ref: main

      - uses: graalvm/setup-graalvm@5298d94fb55a4f185c602eeac5de1b553882abe2 # v1
        with:
          java-version: '25'
          distribution: 'mandrel'
          cache: maven
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set nightly version
        run: mvn versions:set -DnewVersion="nightly-${{ needs.prepare.outputs.version }}" -DgenerateBackupPoms=false -q

      - name: Build native executable
        run: mvn clean package -Dnative -DskipTests -B

      - uses: actions/upload-artifact@v7
        with:
          name: native-arm64
          path: |
            target/*-runner
            target/*.properties
            target/*.so
          if-no-files-found: warn
          retention-days: 1

  # ── Push native + compat Docker images ───────────────────────────────────
  push-native:
    name: Push native and compat Docker images
    needs: [prepare, build-native-amd64, build-native-arm64]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v7.0.1
        with:
          ref: main

      - uses: actions/download-artifact@v8
        with:
          name: native-amd64
          path: native/amd64/

      - uses: actions/download-artifact@v8
        with:
          name: native-arm64
          path: native/arm64/

      - name: Make binaries executable
        run: chmod +x native/amd64/*-runner native/arm64/*-runner

      - name: Set created timestamp
        id: created
        run: echo "created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

      - name: Set SOURCE_DATE_EPOCH
        id: source_date
        run: echo "epoch=$(git log -1 --pretty=%ct)" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push native nightly image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        env:
          SOURCE_DATE_EPOCH: ${{ steps.source_date.outputs.epoch }}
        with:
          context: .
          file: docker/Dockerfile.native-package
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.LCS_REPO }}:nightly
            ${{ env.LCS_REPO }}:nightly-${{ needs.prepare.outputs.version }}
          labels: |
            org.opencontainers.image.version=nightly-${{ needs.prepare.outputs.version }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.created.outputs.created }}
            version=nightly-${{ needs.prepare.outputs.version }}
            build-date=${{ steps.created.outputs.created }}
            vcs-ref=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push compat nightly image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        env:
          SOURCE_DATE_EPOCH: ${{ steps.source_date.outputs.epoch }}
        with:
          context: .
          file: docker/Dockerfile.compat
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.LCS_REPO }}:nightly-compat
            ${{ env.LCS_REPO }}:nightly-${{ needs.prepare.outputs.version }}-compat
          build-args: |
            VERSION=nightly-${{ needs.prepare.outputs.version }}
          labels: |
            org.opencontainers.image.version=nightly-${{ needs.prepare.outputs.version }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.created.outputs.created }}
            version=nightly-${{ needs.prepare.outputs.version }}
            build-date=${{ steps.created.outputs.created }}
            vcs-ref=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
